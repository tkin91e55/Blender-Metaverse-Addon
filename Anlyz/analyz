#these are overriden
def fbx_data_from_scene(scene, depsgraph, settings):
def fbx_data_material_elements(root, ma, scene_data):
def fbx_data_texture_file_elements(root, blender_tex_key, scene_data):
def fbx_objects_elements(root, scene_data):
def save(operator, context,
def save_single(operator, scene, depsgraph, filepath="",
#This are short and custom
def fbx_metav_toolset_data_material_elements(root, ma, scene_data):
def fbx_metav_toolset_data_texture_file_elements(root, blender_tex_key, scene_data):

Call flow

def save_single(operator, scene, depsgraph, filepath="",
  fbx_data_from_scene(scene, depsgraph, settings):
    HifiShaderWrapper
  fbx_objects_elements(root, scene_data)
    fbx_data_material_elements(objects, ma, scene_data)
      fbx_metav_toolset_data_material_elements(root, ma, scene_data):
        HifiShaderWrapper(PrincipledBSDFWrapper):
    fbx_data_texture_file_elements(objects, blender_tex_key, scene_data)
      fbx_metav_toolset_data_texture_file_elements(root, blender_tex_key, scene_data)
        HifiShaderWrapper(PrincipledBSDFWrapper):